<?php
class GridAppController extends AppController {
	// var $helpers = array('Html', 'Text', 'Time', 'core.PHTime');
	/*
	function getFilters() {
		// $aParams = $this->params['named'];
		$conditions = $this->params['named'];
		unset($conditions['page']);
		unset($conditions['sort']);
		unset($conditions['direction']);
		unset($conditions['limit']);
		return $conditions;
	}
	*/
}
